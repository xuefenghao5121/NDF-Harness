# <短标题：一周期一事>

> schema: ndf-genesis-idea/v1
> track: bootstrap
> bootstrap_mode: greenfield
> cycle_id: <kebab-case，全仓唯一>
> maps_to: skeleton
> status: draft

## Verbatim intent

> <Copy the human's original wording verbatim. Do not improve or paraphrase.>

## Problem and user

- Problem:
- Target user:
- Existing alternative:

## In-scope (this cycle)

1. <Product capability for this cycle only; no "establish skeleton"; no subtask IDs>
2.
3.

## Out-of-scope (this cycle)

1. <sibling cycle_id: …> — <one-line why deferred>
2. <sibling cycle_id: …> — <one-line why deferred>

## Success (not SLA)

| Condition | Measure | Status |
|-----------|---------|--------|
| | | draft |

## Hard constraints

- Platform:
- Language/runtime:
- Performance comparison target (name only, no numeric SLA):
- Other:

## Guidance (non-normative)

本栏不是契约。Control MUST NOT 将本栏写入 BEH/API/CON-SLA must.

- <optional algorithm hints, links, deferred detail — feeds synthesis, not spec directly>

## Mapping (synthesis inputs)

| 输入栏目 | 进入 synthesis | 禁止 |
|----------|----------------|------|
| Verbatim + In-scope | DESIGN_MAP 产品范围 | 直接写 spec/00–50 |
| Out-of-scope | DESIGN_MAP 排除项 | 忽略 sibling |
| Success | DESIGN_MAP 验证/性能 tbd 注记 | NDF 落盘/门禁/条款数 |
| Hard constraints | DESIGN_MAP 约束面 | ISA 清单当 must |
| Guidance | DESIGN_MAP exclusions 引用 | 复制为 BEH/API/CON-SLA must |

Control `hop=genesis_synthesis` 写 DESIGN_MAP；`hop=genesis_design` 只从 **已审核**
DESIGN_MAP 物化 spec/00–50。
